# Multi-Agent Harness

A TypeScript runtime for building, running, and composing AI agents — including agents that write agents.

## Structure

- `runtime/` — core agent runtime: session management, sandbox execution, tool/toolkit system
- `docs/` — high-level design documentation
