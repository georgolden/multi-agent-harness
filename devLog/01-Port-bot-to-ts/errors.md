Issue:

It missed versions from package.json of dependencies

Fix:

It should check dependencies and version in web or have a file that contains all docs links for each dependency and attouch it to context

Issue:

It is unable to retrieve typescript types

Fix:

For my coding agent add type signatures retrieval. For this - ask to check types in node modules
